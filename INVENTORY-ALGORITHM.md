# 材料庫存系統演算法邏輯

> 對應檔案：`inventory.html`（前端）、`functions/main.py`（Cloud Function 同步）
> 最後更新：2026-07-28

| 章節 | 內容 |
|---|---|
| [一](#一核心公式) | 核心公式——先記這三條 |
| [二](#二材料家族正規化) | 材料家族正規化（最容易踩雷） |
| [三](#三自動消耗扣庫存cloud-function每-30-分鐘) | 自動消耗扣庫存（Cloud Function） |
| [四](#四手動操作如何影響庫存) | 手動操作如何影響庫存 |
| [五](#五換槽扣料模型驗證與對帳) | 換槽扣料模型：驗證與對帳 |
| [六](#六前端資料載入與材料清單) | 前端資料載入與材料清單 |
| [七](#七已知地雷) | 已知地雷 |
| [八](#八驗證指令) | 驗證指令 |
| [九](#九firestore-資料結構速查) | Firestore 資料結構速查 |

---

## 一、核心公式

```
總庫存      = 備料庫存（inv.stock 中同家族所有 key 的 total_ml 加總）
機台樹脂罐   = 純顯示，不計入總庫存
低庫存警示   = 總庫存 < 安全庫存（預設 1000 ml = 1 L）
```

**為什麼樹脂罐不計入總庫存**：樹脂罐裡的樹脂是「已經領出去的料」，消耗發生時 Cloud Function 已經直接從備料庫存扣掉了。如果樹脂罐也算進總庫存，同一批料會被計算兩次。（此設計曾詢問過使用者，明確確認維持現狀。）

單位一律用 **ml** 儲存，顯示時才 ÷1000 轉成 L。耗材（樹脂槽、攪拌器）借用同一個欄位，1 個 = 1000，顯示成「個」。

---

## 二、材料家族正規化

Formlabs 的材料代碼是 8 碼（`FL` + 6 碼），最後 2 碼是版本號：

```
FLTO2001  →  家族 FLTO20（Tough 2000）、版本 01
FLTO2002  →  家族 FLTO20（Tough 2000）、版本 02
```

系統把**同家族的所有版本視為同一種材料**加總，所以 `familyCode()` 是所有計算的入口：

```
familyCode(code):
    1. 在 FAMILY_REMAP 表裡          → 直接回傳對照值
    2. 符合 /^FL[A-Z0-9]{6}$/ 且含數字 → 取前 6 碼，再查一次 FAMILY_REMAP
    3. 其他（材料名稱、自訂名稱）      → 原樣回傳
```

**「且含數字」這個條件是必要的**：`FLEXIBLE`、`FLAMERET` 這類名稱也符合 `FL` + 6 碼，沒有數字檢查就會被誤截成錯誤的家族代碼。`FAMILY_REMAP` 裡的 `FLEXIB`、`FLAMER` 就是舊版本誤截留下的殘骸，靠對照表救回來。

`FAMILY_REMAP` 目前的內容：

| 來源 | 併入 | 原因 |
|---|---|---|
| `FLEXIB` | `FLFL80` | 舊版誤截 "Flexible 80A" |
| `FLAMER` | `FLFRGR` | 舊版誤截 "Flame Retardant" |
| `FLRGWH` | `FLRG40` | 使用者確認 FLRGWH 與 Rigid 4000 是同一材料 |

### ⚠️ 前後端必須同步

`inventory.html` 的 `familyCode()` 和 `functions/main.py` 的 `family_code()` 是同一套邏輯的兩份實作。純前端專案沒有 build step，JS 與 Python 無法共用程式碼，只能各寫一份。

改了一邊沒改另一邊，會出現「後端扣 A 家族、前端顯示 B 家族」的鬼打牆——而且**不會拋錯，只會默默把庫存算錯**。

對策是每次改完對照表都跑同步檢查（見[第八節](#八驗證指令)）。腳本比對 `FAMILY_REMAP`、`FAMILY_TO_NAME`，以及所有已知代碼／顯示名稱的家族碼解析結果，分兩級：**會影響庫存正確性的差異→ exit 1**，**只影響顯示名稱的差異→提示**。

> 前端從 `FAMILY_TO_NAME` 反轉出來的「裸家族名」（如使用者手動輸入 `Tough 2000`）是前端專屬的輸入便利別名，後端只會收到 Formlabs 代碼或帶版本的顯示名稱，因此**刻意不列入必須一致的範圍**。

---

## 三、自動消耗扣庫存（Cloud Function，每 30 分鐘）

這是整個系統的主要資料來源。

```
1. OAuth 取 token
2. 拉 /printers/ + /cartridges/  →  寫入 printer_status/current（機台即時狀態）
3. 樹脂罐數值同步到 inv.cartridges（純顯示，數值直接取 API 的 initial_ml − dispensed_ml）
4. 逐台機台拉 /prints/?printer={serial}  →  對每一筆 print：
       ├─ guid 已在 last_processed_prints  →  跳過（不重寫，省寫入配額）
       ├─ 狀態未完成（IN_PROGRESS/QUEUED…）  →  跳過
       ├─ 無材料或無用量  →  跳過
       ├─ 判斷 record_type：完成/錯誤 → consume，中止 → aborted
       └─ 判斷 will_deduct（是否真的扣庫存）
5. 批次寫 inventory_history（doc_id = print guid，天然防重複）
6. 套用扣減到 inv.stock
```

### 「這筆要不要扣庫存」的三個條件

```
will_deduct = (不是 backfill 模式)
           && (guid 不在 deducted_prints 裡)     ← 每筆 print 只扣一次
           && (不是舊版本代碼)                    ← 見下
```

判斷結果寫進歷史紀錄的 `stock_deducted` 欄位，**刪除紀錄要不要回補庫存就是看這個欄位**。

### 版本規則：消耗以最新版本計算

如果某個家族已經看過 `FLTO2002`，那麼 `FLTO2001` 的消耗**只記錄、不扣庫存**。

理由：備料庫存裡放的是新版本的料，舊版罐子（可能還插在機台上沒用完）的用量不該去扣新版的庫存數字。

系統自動追蹤每個家族看過的最新版本（`family_latest_version`），不需要手動維護。判斷採保守策略——**解析不出版本號、或該家族還沒看過更新版本，一律照常扣**。

有一個特例對照表 `VERSION_ALIAS`：`FLRG1011` 和 `FLRG1002` 在 Formlabs 對照中都是 "Rigid 10K V1.1"，但末 2 碼是 `11` vs `02`，直接比大小會把 `FLRG1002` 誤判成舊版而不扣庫存，所以把它拉平成同一個版本號。

### ⚠️ 版本號一旦正規化就永久丟失

`raw_material`（截斷前的原始代碼）只在 `main.py` 處理當下短暫存在，`canon_material()` 一跑完就只剩家族代碼。版本追蹤必須掛在截斷之前，而且**只能影響之後同步進來的新資料**。

這造成兩個後果，處理方式不同：

**(a) 庫存沒有版本維度 → 舊版庫存永遠扣不掉。**
實體上有 2 L 的 V2 加 1 L 的 V1，系統只會顯示「Tough 2000 3 L」。V1 的列印依規則不扣庫存，所以那 1 L 在帳上永遠不會減少，永久虛增家族總量。
要根治得把 `inv.stock` 的 key 改成帶版本，會動到整個系統。**目前的做法是接受這個限制**，混版時靠人工盤點調整。

**(b) 舊歷史紀錄沒有 `material_raw` → 但可以救回來。**
`sync_formlabs_manual` 的 backfill 模式會重新向 Formlabs API 抓所有 prints 並重建 history，這時就會補上 `material_raw`。backfill 不扣庫存（`will_deduct` 一律 False），對庫存數字是安全的。

> 📌 backfill 重建時，`stock_deducted` 會以 `deducted_prints`（backfill 模式下不會被清空）為準寫回**當初是否真的扣過**的歷史事實，而不是一律寫 `false`。否則這些紀錄日後被刪除時不會回補，等於用 backfill 救回版本資訊卻弄壞了回補判斷。

### 扣減方式：扣到 0 為止，不會變負數

```
對每個要扣的材料：
    找出 inv.stock 裡所有同家族的 key（可能有舊代碼、名稱、家族碼混在一起）
    依序從有量的 key 扣，扣到 0 就換下一個
    全部扣完還有剩 → 記進 stock_shortfalls（累計）
```

`stock_shortfalls` 會讓前端跳出「消耗超過庫存」警示橫幅。這代表帳對不上，常見原因：重複紀錄、材料歸屬錯誤、漏登入庫。使用者查明後可以從前端手動清除。

（早期版本這個差額是靜默丟棄的，前端只看得到庫存卡在 0，完全看不出有一筆消耗沒被反映。）

### 消耗紀錄的時間怎麼決定

```
print_finished_at → finished_at → updated_at → created_at → 現在時間
```

依序取第一個「有效」的時間。**年份 < 2000 視為無效**——Formlabs 偶爾會對 FINISHED 的 print 回傳 epoch(1970) 的 `print_finished_at`，直接採用會把紀錄打到 1970，然後被前端的 30 天視窗濾掉，看起來就像漏抓。

---

## 四、手動操作如何影響庫存

| 操作 | 對庫存的影響 | 歷史紀錄類型 |
|---|---|---|
| **入庫**（備料／耗材） | `total_ml` 增加 | `stockin` |
| **批次編輯數量** | `total_ml` 設為輸入值，記錄差額 | `manual` |
| **換樹脂槽**（耗材數量減少） | 扣掉對應材料的最小預留量 | `manual` |
| **刪除消耗紀錄** | 有條件回補（見下） | `manual`（回補時另記一筆） |

### 換樹脂槽扣料

| 耗材 | 每個扣除 |
|---|---|
| Formlabs Form 4 Resin Tank | 400 ml |
| Formlabs Form 4L Resin Tank | 1000 ml |

**為什麼是這兩個數字**：每次啟用一個新樹脂槽，都必須先倒入一個「最小列印材料量」才能開印——Form 4 約 400 ml、Form 4L 約 1000 ml。這筆料進了槽就出不來（槽報廢時裡面剩的也是這個量），如果要精確追蹤「槽內存量」會讓整個材料管控複雜度大幅上升。因此系統採取簡化做法：**直接把最小預留量當作換槽當下的一次性消耗**。模型的正確性驗證見[第五節](#五換槽扣料模型驗證與對帳)。

只有「樹脂槽」有這個行為，攪拌器（Mixer）沒有。數量減少時會跳出對話框問「這個槽是裝什麼材料」，選「其他」則不扣任何材料、只留一筆備註紀錄供稽核。

> 📌 **扣減時機是「從庫存領出新槽」，不是「丟棄舊槽」。**
> 程式是在耗材數量**減少**時觸發扣減，而耗材庫存數量減少 = 從庫存拿了一個新槽出來用。這個時機恰好對應「倒入 400 ml 預留量」的物理動作，帳才會對。
> 舊版註解寫的「槽內殘留樹脂報廢」是同一筆 400 ml 從循環另一端看的說法——穩態下兩種講法數字相同，但**維護時請以「領新槽 → 扣預留量」為準**，否則容易在「退役但不換新」的情境下多扣一次。

扣不完時（庫存不足、只能扣到 0），差額會**比照 Cloud Function 累計寫進 `stock_shortfalls`**，跳出警示橫幅留下可稽核的證據，要人工查明後才能清除。早期版本前端只跳一個關掉就消失的 toast，歷史紀錄又只記「實際扣掉多少」、不記「原本要扣多少」，異常會整個消失。

換的如果是 **Form 4L** 的槽，該材料會自動被標記為「L 樹脂槽」（Form 4 的小槽不標記）。這個標記也可以由 admin／主管在庫存表格手動開關——但要**進入編輯模式打勾、按「儲存全部」**才真的寫入，不是點一下就存檔。

> 批次儲存的順序很講究：**所有會跳對話框的詢問都在「套用任何變更之前」問完**。使用者中途按取消時要能整批放棄，不能發生「材料已寫入、耗材卻沒寫入」的半套狀態。

### 刪除消耗紀錄的回補條件

```
會回補 ⟺ ml > 0
      && type ≠ 'manual'          ← 手動調整紀錄不代表真實列印消耗
      && stock_deducted ≠ false   ← 當初真的扣過才能回補
```

第三個條件是關鍵：如果當初因為「舊版本」而沒扣庫存，刪除時回補就會**憑空多出庫存**。舊紀錄沒有 `stock_deducted` 這個欄位（`undefined`），視為 `true` 沿用舊行為。

> ⚠️ **`inventory_history` 的 doc 一律要走 `mapHistoryDoc()` 轉換，不要在各處自己寫 `.map()`。**
> 這條規則是踩過坑才有的：訂閱路徑與「載入更早一個月」的補撈路徑原本各自寫了一份欄位清單，兩份都漏掉 `stock_deducted`，導致 `h.stock_deducted` 永遠是 `undefined`。後果是整個保護機制形同虛設——Cloud Function 認真寫了這個欄位，前端判斷式也寫對了，但中間的資料對應層把它丟掉，於是**刪除舊版本紀錄時照樣回補庫存**（正是這個欄位要防的事），而且「未扣庫存」標籤從來沒顯示過。
> 現在兩條路徑共用同一個轉換函式，新增欄位只要改一處。

權限：admin 或主管（有 `delete_board` / `delete_issues`）。`firestore.rules` 的 `inventory_history` 刪除規則與前端 `canDeleteRecords()` 必須一致。

---

## 五、換槽扣料模型：驗證與對帳

以下用物質守恆推導 + 模擬驗證過（涵蓋 1～200 次換槽循環）。

### 單一循環的物質流

```
領新槽    ：庫存 −400        （倒入預留量）
列印 ×N   ：庫存 −ΣP        （卡匣自動回填槽，維持液面）
舊槽退役  ：庫存 不變        （那 400 ml 早在領新槽時就離開庫存了）
────────────────────────────────
物理總計  ：庫存 −(400 + ΣP)
系統扣減  ：−400（換槽）+ −ΣP（Cloud Function 逐筆扣）= −(400 + ΣP)  ✅ 完全吻合
```

**結論 1：沒有重複扣款。** Formlabs API 的 `volume_ml` 只算「固化進成品的樹脂」，不含倒進槽裡的預留量；換槽扣的 400 ml 正好補上這個 API 看不到的缺口。兩個機制互補而非重疊。

**結論 2：理想條件下誤差為零，且不隨次數累積。** 模擬 1／10／50／200 次換槽，drift 全部是 `0.0 ml`。這個模型本身沒有內建的累積性偏差。

**結論 3：誤差不會自我修正，系統性偏差會線性累積。** 系統沒有任何自動對帳機制，一旦每次都偏同一個方向，誤差就是 `次數 × 每次偏差`：

| 每次實際注入量偏差 | 10 次後 | 25 次後 | 50 次後 | 100 次後 |
|---|---|---|---|---|
| +20 ml（+5%） | 0.2 L | 0.5 L | **1.0 L** | 2.0 L |
| +50 ml（+12.5%） | 0.5 L | **1.25 L** | 2.5 L | 5.0 L |
| +100 ml（+25%） | **1.0 L** | 2.5 L | 5.0 L | 10.0 L |

> 換算：每次偏差 20 ml → 50 次換槽累積 1 L；偏差 50 ml → 20 次；偏差 100 ml → 10 次。

**結論 4：純隨機誤差無害。** 若實際注入量只是隨機在 400 ml 上下浮動、沒有固定偏向，誤差走的是隨機漫步（成長 ∝ √N 而非 N）。模擬 σ=30 ml 的情況，100 次換槽後平均 drift 僅 −0.6 ml、標準差 ±325 ml——遠比系統性偏差溫和。**關鍵區別在「有沒有固定偏向」，不在「準不準」。**

**結論 5：Form 4L 的絕對誤差放大 2.5 倍。** 基數從 400 變 1000，同樣 5% 的比例偏差在 4L 上是 50 ml/次（Form 4 是 20 ml/次）。4L 換槽較不頻繁，但單次影響大，值得優先確認實際注入量。

### 會讓誤差累積的四種情況（依風險排序）

| # | 情況 | 方向 | 每次量級 | 緩解方式 |
|---|---|---|---|---|
| 1 | **換同材料時回收舊槽樹脂再利用**（過濾後倒回新槽） | 帳面**虛低** | −400×回收率 | 若確實會回收，換槽時改選「其他」手動填實際用量 |
| 2 | **實際最小注入量不是 400/1000** | 帳面虛高／虛低 | ±偏差值 | 實測一次真實注入量，偏差大就改常數 |
| 3 | **槽未使用就報廢**（破損、誤領） | 帳面虛低 | −400 | 選「其他」不扣料 |
| 4 | **盤點修正耗材數量**（非真實換槽） | 帳面虛低 | −400 | 選「其他」不扣料 |

> 情況 1 最需要留意：模擬顯示回收率 30% 時，25 次換槽就會累積 **−3 L** 的帳面虛低（系統以為料比實際少，會一直誤跳叫料警示）。這不是模型錯，是模型假設「槽內樹脂全報廢」與實際作業不符——**若實務上會回收舊槽樹脂，這個模型就需要調整**。

### 建議的對帳頻率

系統唯一的真實對帳手段是**實體盤點後用「批次編輯庫存」把數字改成實際值**（會記一筆 `manual` 差額紀錄，可回溯）。

以預設安全庫存 1 L 為基準，建議讓累積 drift 控制在 0.5 L 以內：

- **正常情況（無系統性偏差）**：每 **20～30 次換槽**盤點一次即可
- **不確定實際注入量是否為 400/1000**：先連續盤點 3～5 個換槽週期，用差額反推真實預留量，確認後再放寬
- **會回收舊槽樹脂**：不適用固定扣減，應改用「其他」逐次填實際值

另有一個下游徵兆可當免費的異常偵測：若某材料頻繁觸發 `stock_shortfalls`（消耗超過庫存），通常代表帳面虛高已經累積到吃光庫存，該做盤點了。

---

## 六、前端資料載入與材料清單

### 資料載入策略

```
onSnapshot 訂閱：最近 30 天的 inventory_history（即時更新）
             +
手動往前補撈：「載入更早一個月」按鈕、月度分析選「全年」時自動補撈
```

補撈的資料存在 `extraHistory`（**只存在本次 session，重新整理就沒了**），和訂閱來的 `historyFromSubscription` 用 Map 依 id 去重後合併成 `inv.history`。

補撈只會抓「目前已載入範圍以外的缺口」，已經涵蓋的區間不重撈，避免浪費 Firestore 讀取額度。

兩條路徑都必須經過 `mapHistoryDoc()` 轉換（原因見[第四節](#刪除消耗紀錄的回補條件)）。

### 材料清單怎麼來的

`getAllMaterials()` 從三個來源收集，全部正規化成家族代碼後去重：

```
機台樹脂罐（inv.cartridges）
     +
備料庫存 key（inv.stock，排除耗材）
     +
歷史紀錄（inv.history）
     ↓
過濾掉「停用」的 + 過濾掉「非材料品項」
```

**停用判斷的優先順序**：

```
1. 在 disabled_overrides 裡（使用者明確要求顯示）  →  永不停用
2. 在 disabled_materials 裡（使用者主動停用）      →  停用（整個家族）
3. 在 DEFAULT_DISABLED_NAMES 裡（系統預設）        →  停用
```

**非材料品項**（`Form 4 Mixer`、`Form 4 Resin Tank` 等機台配件）永遠不算材料，即使不小心從 cartridges/history 混進來也會被擋掉。它們走獨立的「耗材」庫存清單（`kind: 'consumable'`），不做 Formlabs 家族代碼比對。

---

## 七、已知地雷

1. **`.set()` 即使內容不變也計費一筆寫入**
   `perform_sync` 對已在 `last_processed_prints` 的 guid 必須 `continue` 跳過。曾因每輪重寫全部 ~777 筆 history × 每天 144 次 ≈ **11 萬寫入/天**（免費額度只有 2 萬/天）爆量。要強制重建 history 只能用 `sync_formlabs_manual` 的 backfill 模式。

2. **消耗抓取用 `prints/?printer={serial}` 按機台過濾、不加 date、不加 sort**
   改回「date 過濾 + sort 全抓」會漏掉最新一筆。目前是 per-printer 分頁抓取後用 guid 去重。

3. **前後端家族正規化必須同步**（見[第二節](#-前後端必須同步)）
   改完材料對照表跑 `python tools/check_material_sync.py`，不一致會 exit 1。

4. **版本號在寫入 Firestore 前就丟失**（見[第三節](#-版本號一旦正規化就永久丟失)）
   舊版庫存在帳上永遠扣不掉，只能靠人工盤點；舊紀錄的 `material_raw` 可用 backfill 救回。

5. **入庫時輸入無法對應到 Formlabs 家族代碼的名稱**會建立獨立庫存，日後正式代碼同步進來不會自動併入，庫存被拆成兩筆。前端有防呆確認對話框攔一次。

6. **換槽扣料的 400/1000 ml 是固定常數，沒有自我修正機制**
   模型本身經驗證正確（見[第五節](#五換槽扣料模型驗證與對帳)），但若實際最小注入量與常數有固定偏差、或作業上會回收舊槽樹脂，誤差會**線性累積**。需靠定期實體盤點對帳。

7. **讀取 `inventory_history` 一律走 `mapHistoryDoc()`**（見[第四節](#刪除消耗紀錄的回補條件)）
   自己寫 `.map()` 漏欄位不會報錯，只會讓下游判斷靜默失效。

---

## 八、驗證指令

改完對應範圍後請執行（repo 根目錄）：

**材料對照表前後端同步**（改過任一邊的對照表必跑）

```bash
python tools/check_material_sync.py
```

**Cloud Function 語法**

```bash
python -m py_compile functions/main.py
```

**前端 module JS 語法**（抽出 `<script type="module">` 後用 node 檢查）

```bash
python -c "import re;h=open('inventory.html',encoding='utf-8').read();m=re.search(r'<script type=\"module\">(.*?)</script>',h,re.S);open('_inv.mjs','w',encoding='utf-8').write(m.group(1))" && node --check _inv.mjs && rm _inv.mjs
```

---

## 九、Firestore 資料結構速查

### `inventory/main`（單一文件）

| 欄位 | 說明 |
|---|---|
| `stock` | `{家族碼: {total_ml, bottles, note, kind}}` — 備料／耗材庫存，`kind:'consumable'` 為耗材 |
| `cartridges` | `{機台alias: [{slot, material, remaining_ml, ...}]}` — 純顯示 |
| `safety` | `{家族碼: ml}` — 安全庫存，未設定則用預設 1000 |
| `family_latest_version` | `{家族碼: 最新原始代碼}` — 版本判斷依據 |
| `last_processed_prints` | 已寫過 history 的 guid（保留最近 2000 筆） |
| `deducted_prints` | **已扣過庫存**的 guid（保留最近 2000 筆） |
| `stock_shortfalls` | `{家族碼: {ml, last_at}}` — 扣不掉的累計差額 |
| `l_tank_materials` | 標記有 Form 4L 樹脂槽的家族碼 |
| `disabled_materials` / `disabled_overrides` | 停用 / 強制顯示 |
| `partno` / `matNames` | 手動品號 / 手動顯示名稱覆寫 |

### `inventory_history/{print_guid}`（doc_id = guid，天然防重複）

| 欄位 | 說明 |
|---|---|
| `ts` / `tsDate` | 時間字串 / Timestamp（查詢用） |
| `type` | `consume` / `aborted` / `stockin` / `manual` |
| `material` | 家族代碼 |
| `material_raw` | 原始代碼（未截斷版本），舊紀錄沒有 |
| `stock_deducted` | 這筆是否真的扣過庫存 → **刪除時是否回補的依據**（讀取務必經 `mapHistoryDoc()`） |
| `deduct_skip_reason` | `outdated_version` / `backfill` / `null` |
| `ml` | 用量 |
| `note` | 列印檔名或說明（也是「工作類別」分析的解析來源） |

> 「工作類別」從備註的第 2 段解析：`客戶簡稱-工作類別-EF單號`（例：`鑫元鴻-代工-202607020001`）。解析不到就歸「未分類」。
