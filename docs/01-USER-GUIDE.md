# SWTC 3D 列印系統 — 使用說明

> **版本**：v2.2（portal.html 統一入口 + quote-studio 估價系統）
> **更新日期**：2026/07/20
> **負責人**：jiliao

---

## 一、系統概覽

本系統是公司內部 3D 列印機（Formlabs Form4 / Form4L 等）的整合管理工具，**統一入口為 `portal/portal.html`**。

### 功能模組

| 模組 | 入口 | 說明 |
|------|------|------|
| **工作看板** | portal.html 內建 | 訂單進度追蹤，實際消耗量可從庫存消耗紀錄自動帶入 |
| **異常與資源** | portal.html 內建 | 異常紀錄、IPA / 設備維護 |
| **後台管理** | portal.html 內建 | 使用者管理、系統設定 |
| **3D列印機預約** | portal.html → iframe | 機台預約、甘特圖（支援跨天）、即時狀態 |
| **材料庫存管理** | portal.html → iframe | 樹脂罐、備料庫存、消耗紀錄、月度分析 |
| **3D列印估價（Beta）** | portal.html → iframe | quote-studio.html，3D模型估價、STL分析報告、材料/價格設定 |

---

## 二、使用者角色

| 角色 | 預約系統 | 庫存系統 | 工作看板 / 異常 | 估價系統 | 後台管理 |
|------|---------|---------|----------------|---------|---------|
| **viewer** | 查看所有預約、機台狀態 | 查看庫存、消耗紀錄 | 查看 | 可使用估價 | — |
| **editor（工程師）** | viewer ＋ 新增/編輯/刪除預約 | viewer ＋ 編輯庫存、新增入庫 | 新增/編輯 | 可使用估價 | — |
| **主管（manager）** | editor 全部 | editor ＋ **刪除消耗紀錄**（自動回補庫存） | editor 全部 ＋ 刪除 | editor 全部 ＋ **材料/價格設定** | — |
| **admin** | editor 全部 | editor ＋ 觸發同步、安全庫存設定、去重、刪除消耗紀錄 | 全部 ＋ 刪除 | 全部 ＋ 材料/價格設定 | ✅ |

新使用者建立後預設為 viewer，admin 至後台管理頁面的「使用者」分頁設定權限，或於「角色權限」分頁調整各角色的預設權限範圍。**系統至少需保留一位 admin**：若目前只剩一位管理員，刪除該帳號或移除其管理員權限會被系統擋下。

---

## 三、功能模組說明

### 3.1 工作看板

追蹤訂單從報價到出貨的各階段進度。

- **新增訂單**：填入訂單編號、客戶、機型、狀態
- **狀態欄**：可拖拉或點選切換（報價中 / 確認 / 生產中 / 出貨 / 完成）
- **排序**：可按各欄位排序

### 3.2 異常與資源

分三個子頁籤：

| 子頁 | 說明 |
|------|------|
| **異常紀錄** | 列印異常、設備故障等事件 |
| **IPA 耗材** | IPA 入庫 / 耗用紀錄 |
| **設備維護** | 定期保養、維修紀錄 |

### 3.3 後台管理（admin only）

- 使用者清單：查看所有帳號、角色
- 系統設定：workspace 全域設定

### 3.4 3D 列印機預約

#### 新增預約（editor / admin）

1. 點頂部「+ 新增預約」
2. 填入：日期、業務、機台、客戶/案號/備註、是否已下單、目的、負責工程師、狀態
3. 點「儲存」

#### 查看預約

- **甘特圖模式**（預設）：按機台分線顯示時間軸
- **表格模式**：列表，可排序、過濾

#### 即時機台狀態

頁面下方顯示機台即時狀態（IDLE / PRINTING / ERROR）、材料與剩餘量。Cloud Function 每 30 分鐘同步，前端 onSnapshot 即時推送，**不需手動重整**。

### 3.5 材料庫存管理

#### 四個分頁

| 分頁 | 內容 |
|------|------|
| **庫存總覽** | 月度消耗統計、低庫存警告、機台樹脂罐狀態 |
| **材料庫存** | 備料庫存清單、機台樹脂罐（摺疊）、可編輯 |
| **消耗紀錄** | 列印消耗、入庫、調整的歷史，預設 30 天 |
| **月度分析** | 圓餅圖、月度趨勢圖、詳細統計表 |

#### 材料庫存頁欄位說明

| 欄位 | 來源 | 可編輯 |
|------|------|-------|
| **材料名稱** | 系統內建 + 自動偵測新材料；同家族材料若偵測到新版本代碼會自動改顯示最新版本名稱 | 否 |
| **庫存 (L)** | `inv.stock[material].total_ml / 1000` | ✓ editor 編輯模式 |
| **機台上 (L)** | Cloud Function 同步自 Formlabs API | 否（自動更新） |
| **安全庫存 (L)** | 手動設定 | ✓ admin only |
| **狀態** | 庫存 < 安全庫存 → 需叫料 | 否（自動） |
| **備註** | 手動輸入 | ✓ editor 編輯模式 |

#### 整頁編輯模式（editor / admin）

1. 點右上「✏️ 編輯」
2. 所有列的「庫存 (L)」和「備註」啟用（藍框）
3. 編輯完後點「✓ 儲存全部」→ 一次寫入 Firestore
4. 或點「✕ 取消」→ 還原

#### 新增入庫

點「📦 新增入庫」→ 選材料 + 數量 → 儲存。自動寫入一筆 history（type=stockin）。

#### 消耗紀錄

預設訂閱**最近 30 天**。點「⏮ 載入更早一個月」往前推 30 天（F5 後回預設）。

**紀錄類型：**

| Type | 顯示 | 說明 |
|------|------|------|
| `consume` | 🔴 列印消耗 | 列印完成 / 列印錯誤 |
| `aborted` | 🟠 列印中止 | 列印被取消 |
| `stockin` | 🟢 備料入庫 | 手動新增入庫 |
| `manual` | ⚙️ 手動調整 | 編輯庫存的差異紀錄 |

#### 備註格式慣例（影響工作看板自動帶入消耗量）

編輯消耗紀錄備註時建議填：**客戶簡稱-工作類別-EF單號**（例：`鑫元鴻-代工-202607020001`）。工作類別需為「代工」或「評估」其中之一，工作看板才能依 EF 單號自動比對、加總消耗量帶入「實際消耗量」欄位。

#### 刪除消耗紀錄（admin / 主管）

消耗紀錄旁的垃圾桶圖示可刪除該筆紀錄；若該筆是「列印消耗」或「列印中止」類型，刪除時會**自動把用量加回同家族備料庫存**，並留一筆「回補」歷史紀錄註明操作者。手動調整類型（`manual`）的紀錄本身不代表真實消耗，刪除不會動庫存數字。

---

## 四、3D 列印估價（quote-studio.html，Beta）

原本的 `quote.html` 已下線，往後估價一律使用沉浸版 `quote-studio.html`（portal 側邊欄「3D列印估價」，標有 BETA 徽章）。

### 4.1 基本流程

1. 上傳 STL/OBJ/3MF 模型
2. 選擇機台、材料、層厚
3. 擺放模型（可用旋轉環手動調整、或一鍵自動擺放）
4. 檢視 STL 分析報告（水密檢查、厚度、懸垂、孤島等），孤島警告可點擊在 3D 視圖標示位置
5. 產生報價單、可存成工單

### 4.2 常用操作

| 功能 | 說明 |
|------|------|
| **底視 / 360° 旋轉** | 視角列有「底視」按鈕；拖曳旋轉視角已放寬到接近整圈，可以看到模型底部 |
| **旋轉環吸附 1°** | 拖曳旋轉環預設以 1 度為單位吸附，按住 Shift 可自由角度 |
| **無支撐選項** | 材料與參數步驟可切換「有支撐 / 無支撐」，選無支撐時成本不會再套用預設的 15% 粗估支撐量 |
| **全螢幕檢視** | 工具列「⛶ 全螢幕」只會隱藏 portal 側欄填滿瀏覽器視窗，不會用瀏覽器原生全螢幕（保留書籤列、工作列） |
| **黑暗模式** | 工具列「🌙 黑暗」切換全站配色，會記住偏好 |
| **工作類型倍率** | 成本分析面板可切換「代工 / 評估」，套用 admin/主管設定的計費倍率 |

### 4.3 材料與價格設定（admin / 主管專屬）

側邊工具列「⚙ 設定」（僅 admin 或有 `manage_quote_pricing` 權限的主管可見）：

- **材料清單**：可新增/編輯/刪除材料（名稱、單價、密度），與 `quote.html` 舊版材料設定共用同一份 Firestore 清單
- **計費倍率**：分別設定「代工」「評估」兩種倍率，套用在成本小計（材料＋支撐＋後處理）之上

---

## 五、管理員（admin）專屬功能

### 庫存系統 admin 按鈕

| 按鈕 | 用途 |
|------|------|
| **🔄 觸發同步** | 立即執行 Cloud Function（一般每 30 分鐘自動） |
| **🗑 清除並重建** | 清空所有 history + last_processed_prints，重新 backfill |
| **🚫 去除重複** | 按 print_guid 去重，刪除多餘紀錄 |
| **⚙️ 安全庫存設定** | 設定每個材料低庫存警告閾值 |
| **🚫 隱藏材料設定** | 把舊版本材料從清單隱藏 |

---

## 六、典型使用情境

### 情境 A：列印機完成一筆列印

1. Formlabs 機台列印完成
2. **30 分鐘內** Cloud Function 自動偵測並寫入 Firestore
3. 前端 onSnapshot 即時更新：
   - 機台狀態卡片更新（IDLE）
   - 消耗紀錄出現新一筆
   - 庫存頁「機台上 (L)」更新

### 情境 B：使用者手動換樹脂罐

1. 使用者拿備料一罐裝到機台上
2. Cloud Function 同步後「機台上 (L)」更新為新罐容量
3. ⚠️ **備料庫存「庫存 (L)」不會自動扣** — 需手動編輯：
   - 材料庫存頁 → ✏️ 編輯 → 該材料庫存減少 1 L → 儲存

### 情境 C：admin 設定安全庫存

1. 材料庫存頁 → 點「⚙️ 安全庫存設定」
2. 設定每個材料閾值（L）→ 儲存
3. 庫存低於閾值的材料顯示「需叫料」紅色標籤

### 情境 D：消耗紀錄出現 1970 年資料

Cloud Function 內建 `parse_valid_ts` 修正：Formlabs 偶爾回傳 epoch(1970) 的 `print_finished_at` 時，自動退回 `created_at` 使用。正常情況下使用者不需要處理。

---

## 七、即時資料對應表

| 看到的數值 | 來源 | 更新頻率 |
|----------|------|---------|
| 預約清單 | Firestore `bookings` | 即時 (onSnapshot) |
| 預約狀態（執行中/已完成） | 依日期即時計算，不寫回 Firestore | 每次畫面渲染即時判斷，已完成/取消不會被自動覆蓋 |
| 工作看板 | Firestore `workboard_orders` | 即時 (onSnapshot) |
| 異常紀錄 | Firestore `issues_anomalies` | 即時 (onSnapshot) |
| 機台即時狀態 | Firestore `printer_status/current` | Cloud Function 30 分鐘寫入 → 即時推送 |
| 機台上樹脂罐剩餘 | Firestore `inventory/main.cartridges` | Cloud Function 30 分鐘寫入 → 即時推送 |
| 備料庫存 (L) | Firestore `inventory/main.stock` | 使用者手動編輯 → 即時 |
| 消耗紀錄 | Firestore `inventory_history`（最近 30 天訂閱） | Cloud Function 30 分鐘寫入 → 即時推送 |

---

## 八、瀏覽器相容性

| 環境 | 支援 |
|------|------|
| Chrome / Edge / Firefox（最新版） | ✅ |
| Safari（最新版） | ✅ |
| Mobile（iOS / Android） | ✅ 響應式佈局 |
| Internet Explorer | ❌ 不支援 |

**強烈建議**更新後按 `Ctrl + Shift + R` 強制清除快取。若仍是舊版，開無痕視窗確認 — 若無痕也是舊的代表 CDN/伺服器端問題，非瀏覽器 cache。
